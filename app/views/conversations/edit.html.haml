.conversation{ data: { controller: :conversations,
                       'user-id' => current_user.id,
                       action: 'my-channel:generateText@window->conversations#onGenerateText' } }
  #conversation-turns
    - @conversation.turns.each do |t|
      = render ConversationTurnComponent.new(t)
  = render PromptFormComponent.new(conversation: @conversation)
