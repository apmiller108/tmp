.c-wysiwyg-editor{ data: { controller: 'wysiwyg-editor',
                  action: 'memo-form:memoFormSubmit@window->wysiwyg-editor#removeAllPlaceholders my-channel:generateText@window->wysiwyg-editor#onGenerateText' }}
  = rich_text_area_tag(rich_text_area_name, rich_text_area_value)
