%nav.c-scrollspy.d-flex.flex-column.align-items-stretch{ data: { controller: :scrollspy, container_id:, target_id:  }, id: target_id }
  %nav.nav.nav-pills.d-flex.flex-column{ id: self.class::ITEMS_CONTAINER_ID }
    - items.each do |item|
      = item
