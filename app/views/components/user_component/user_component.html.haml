%h1 Account
%div
  %p= email
%h2 Settings
%div
  = form_with model: [user, setting] do |form|
    = form.label :text_model
    = form.collection_select :text_model, GenerativeText::Anthropic::MODELS.values, :api_name, :name
    = form.submit 'Save'
