%div{ id: dom_id(message) }
  = form_with model: [current_user, message], data: { turbo_frame: frame_id } do
    = tag.turbo_frame(id: frame_id, class: ['inline-edit']) do
      = message.content
      %div
        = link_to t('messages.edit'), edit_user_message_path(current_user, message)
