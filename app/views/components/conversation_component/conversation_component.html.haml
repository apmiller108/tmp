.c-conversation.scroll-no-bar-container.container-sm.pb-1.h-100.d-flex.flex-column.justify-content-around{id:, class: opts[:class],
                            data: { controller: :conversation,
                                    conversation_id: conversation_form.conversation.id } }
  #conversation-turns.conversation-turns.mb-2{ data: { 'conversation-target': 'turns' } }
    = render ConversationTurnComponent.with_collection(conversation.turns)
  = render PromptFormComponent.new(conversation_form:)
