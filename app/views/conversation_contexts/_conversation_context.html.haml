.context-item.d-flex.justify-content-between.align-items-center.p-2.border.rounded.mb-2{ id: dom_id(context)}
  .context-info.d-flex.align-items-center
    %i.bi.bi-file-earmark.me-2.text-muted
    .context-details
      .file-name.fw-medium= context.filename
      .file-meta.small.text-muted
        = time_ago_in_words(context.created_at)
        ago
  .context-actions
    = link_to conversation_conversation_context_path(context.conversation_id, context),
      id: dom_id(context, 'delete'),
      class: 'fs-5 btn icon-btn icon-btn-sm danger',
      data: { turbo_method: 'delete', turbo_confirm: t('confirm'),
            'bs-toggle' => 'tooltip',
            'bs-title' => t('delete', name: 'context') } do
      %i.bi-trash3
